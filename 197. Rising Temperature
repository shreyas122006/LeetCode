# Write your MySQL query statement below
Select W1.id 
from weather W1, weather W2 # or use join weather w2
where datediff(W1.recordDate, W2.recordDate) = 1 # or ON subdate (w1.recordDate,1) = w2.recordDate
and W1.temperature > W2.temperature
